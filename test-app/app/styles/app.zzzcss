:root {
  --brand-primary: #3490dc;
  --rem-adjustment: 0.625;
}

.ember-skeleton-container {
  font-size: 10px;
  position: relative;
}
.ember-skeleton-container header {
  position: absolute !important;
}
.ember-skeleton-container code {
  background: transparent !important;
}
.ember-skeleton-container .theme-icons-demo .viewport > div {
  margin-bottom: var(--spacer-lg);
  padding-bottom: var(--spacer-lg);
  border-bottom: 1px solid #ccc;
}
.ember-skeleton-container .theme-icons-demo svg {
  max-width: 20px;
  max-height: 20px;
  display: inline-block;
  margin-right: var(--spacer-md);
}
.ember-skeleton-container #example-list-group-colour-variants.viewport ul {
  list-style: none;
}
.ember-skeleton-container #example-list-group-colour-variants.viewport ul li {
  margin-left: 0;
  padding-left: 0;
}
.ember-skeleton-container #example-border-radius.viewport,
.ember-skeleton-container #example-border-widths.viewport,
.ember-skeleton-container #example-border-custom-colour-variants.viewport {
  display: flex;
  flex-wrap: wrap;
}
.ember-skeleton-container #example-border-radius.viewport > div,
.ember-skeleton-container #example-border-widths.viewport > div,
.ember-skeleton-container #example-border-custom-colour-variants.viewport > div {
  background: var(--gray-extra) -light;
  margin: var(--spacer-md);
  padding: var(--spacer-md);
  font-family: var(--monospace-font-family);
}
.ember-skeleton-container #example-border-radius.viewport > div {
  background: var(--gray-dark);
  color: var(--white);
  width: 100px;
  height: 100px;
  display: flex;
  justify-content: center;
  align-items: center;
  font-family: var(--monospace-font-family);
}
.ember-skeleton-container #example-border-custom-colour-variants.viewport {
  display: flex;
  flex-wrap: wrap;
}
.ember-skeleton-container #example-border-custom-colour-variants.viewport > div {
  margin: var(--spacer-md);
  padding: var(--spacer-md);
}
.ember-skeleton-container #example-overlay-colours-white.viewport,
.ember-skeleton-container #example-overlay-colours-black.viewport,
.ember-skeleton-container #example-custom-overlay-colours.viewport {
  padding: var(--spacer-xl);
  background: var(--gray-extra) -light;
  display: flex;
  flex-wrap: wrap;
}
.ember-skeleton-container #example-overlay-colours-white.viewport > div,
.ember-skeleton-container #example-overlay-colours-black.viewport > div,
.ember-skeleton-container #example-custom-overlay-colours.viewport > div {
  padding: var(--spacer-lg);
  margin: var(--spacer-lg);
  font-family: var(--monospace-font-family);
}
.ember-skeleton-container #example-overlay-colours-white.viewport {
  background: var(--gray-dark);
  color: #fff;
}
.ember-skeleton-container .padding-examples.viewport > div {
  background: var(--gray-medium);
}
.ember-skeleton-container .padding-examples.viewport > div > div {
  background: var(--gray-light);
  font-family: var(--monospace-font-family);
}
.ember-skeleton-container .margin-examples.viewport {
  display: flex;
  flex-wrap: wrap;
}
.ember-skeleton-container .margin-examples.viewport > div {
  background: var(--gray-light);
  padding: var(--spacer-lg);
  font-family: var(--monospace-font-family);
}
.ember-skeleton-container .margin-examples.viewport.vertical > div {
  width: 100%;
}
.ember-skeleton-container .height-width-demo .viewport > div {
  background: var(--gray-dark);
}
.ember-skeleton-container .height-width-demo .viewport > div > div {
  background: var(--gray-light);
}
.ember-skeleton-container .last-child-margin-bottom-0-demo.viewport > div {
  padding: var(--spacer-sm);
  background: var(--gray-dark);
}
.ember-skeleton-container .last-child-margin-bottom-0-demo.viewport > div > div {
  background: var(--gray-light);
  padding: var(--spacer-md);
  font-family: var(--monospace-font-family);
}
.ember-skeleton-container .card-box-shadow-example.viewport > div {
  margin-bottom: var(--spacer-lg);
  padding: var(--spacer-lg);
  border-radius: 3px;
  font-family: var(--monospace-font-family);
}
.ember-skeleton-container .box-arrow-example.viewport > div,
.ember-skeleton-container .box-arrow-bg-example.viewport > div {
  display: inline-flex;
  border: 1px solid var(--gray-medium);
  margin-bottom: var(--spacer-lg);
  padding: var(--spacer-xxl) var(--spacer-lg);
  border-radius: 3px;
  font-family: var(--monospace-font-family);
}
.ember-skeleton-container .box-arrow-bg-example.viewport > div {
  color: #fff;
  border: 1px solid var(--gray-dark);
}
.ember-skeleton-container .viewport .alert,
.ember-skeleton-container .viewport .btn {
  margin-bottom: var(--spacer-md);
}

.layout-container .main-content {
  height: 200px;
}
.layout-container .viewport {
  padding: 0;
  border: 1px solid var(--gray-light);
}
/*@ sourceMappingURL=app.css.map*/